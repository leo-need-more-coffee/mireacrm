"""Отчёт об архитектуре Mirea CRM."""

from reportlab.lib import colors
from reportlab.lib.enums import TA_CENTER
from reportlab.lib.styles import ParagraphStyle
from reportlab.lib.units import mm
from reportlab.platypus import PageBreak, Paragraph, Spacer, Table, TableStyle
from reportlab.platypus.tableofcontents import TableOfContents

import diagrams
from build import (
    BODY, CAPTION, CELL, CONTENT_WIDTH, GREY, H1, H2, H3, INK, LEAD, PLAIN,
    Report, bullets, figure, heading, mono, note, numbered, table_block,
)

W = CONTENT_WIDTH
story = []
add = story.append
extend = story.extend


# ---------------------------------------------------------------- титул

TITLE = ParagraphStyle("title", fontName="Body-Bold", fontSize=24, leading=29,
                       alignment=TA_CENTER, textColor=INK)
SUBTITLE = ParagraphStyle("subtitle", fontName="Body", fontSize=13, leading=18,
                          alignment=TA_CENTER, textColor=INK, spaceBefore=8)
SMALL = ParagraphStyle("small", fontName="Body", fontSize=10.5, leading=15,
                       alignment=TA_CENTER, textColor=GREY)

add(Spacer(1, 58 * mm))
add(Paragraph("Mirea CRM", TITLE))
add(Paragraph("Микросервисная система для малого бизнеса сферы услуг", SUBTITLE))
add(Spacer(1, 6 * mm))
add(Paragraph("Описание архитектуры", SUBTITLE))
add(Spacer(1, 30 * mm))

summary = Table(
    [
        [Paragraph("Микросервисов", CELL), Paragraph("8", CELL)],
        [Paragraph("Языки", CELL), Paragraph("Python, Go", CELL)],
        [Paragraph("Синхронные вызовы", CELL), Paragraph("9 методов gRPC", CELL)],
        [Paragraph("Доменные события", CELL), Paragraph("10", CELL)],
        [Paragraph("Брокеры сообщений", CELL), Paragraph("RabbitMQ, NATS", CELL)],
        [Paragraph("Автоматических тестов", CELL), Paragraph("287", CELL)],
    ],
    colWidths=[60 * mm, 50 * mm],
    hAlign="CENTER",
)
summary.setStyle(TableStyle([
    ("VALIGN", (0, 0), (-1, -1), "MIDDLE"),
    ("TOPPADDING", (0, 0), (-1, -1), 3),
    ("BOTTOMPADDING", (0, 0), (-1, -1), 3),
    ("LEFTPADDING", (0, 0), (-1, -1), 0),
    ("LINEABOVE", (0, 0), (-1, 0), 0.7, INK),
    ("LINEBELOW", (0, -1), (-1, -1), 0.7, INK),
]))
add(summary)
add(PageBreak())


# ---------------------------------------------------------------- содержание

toc = TableOfContents()
toc.levelStyles = [
    ParagraphStyle("toc0", fontName="Body-Bold", fontSize=11, leading=17,
                   firstLineIndent=0, leftIndent=0, textColor=INK),
    ParagraphStyle("toc1", fontName="Body", fontSize=10.5, leading=15,
                   firstLineIndent=0, leftIndent=8 * mm, textColor=INK),
]
add(Paragraph("Содержание", H1))
add(toc)
add(PageBreak())


# ---------------------------------------------------------------- 1

add(heading("Назначение системы"))
add(Paragraph(
    "Сеть небольших предприятий сферы услуг ведёт филиалы и специалистов. "
    "Клиенты записываются на услуги, каждая занимает известное время и расходует "
    "материалы. После визита выставляется счёт, специалисту начисляется комиссия, "
    "клиенту — баллы лояльности. Владелец сети смотрит выручку, загрузку "
    "специалистов и расход материалов.", LEAD))
add(Paragraph(
    "Модель нейтральна к нише. Она одинаково ложится на барбершоп, тату-студию, "
    "автосервис, частную клинику и мастерскую по ремонту — везде, где клиент "
    "записывается к специалисту на услугу определённой длительности. Термин "
    "«специалист» выбран именно поэтому: в барбершопе его назовут мастером, "
    "в клинике врачом, в автосервисе механиком, а в модели это одна роль.", BODY))

add(Paragraph("Границы системы", H3))
add(Paragraph(
    "То, чего система намеренно не делает, определяет её не меньше, чем "
    "реализованные функции:", PLAIN))
extend(bullets([
    "эквайринга нет — оплата счёта отмечается вручную;",
    "реальной отправки сообщений нет — адаптеры каналов пишут в журнал;",
    "закупок и поставщиков нет — склад знает остатки, но не знает их происхождения;",
    "интерфейса пользователя нет — система предоставляет программный интерфейс.",
]))
add(Paragraph(
    "Ограничения приняты для того, чтобы объём работы остался учебным, "
    "а архитектурные решения — производственными.", PLAIN))


# ---------------------------------------------------------------- 2

add(heading("Декомпозиция на сервисы"))
add(Paragraph(
    "Разделение выполнено по ограниченным контекстам предметной области, а не по "
    "техническим слоям. В системе нет сервиса «база данных» или сервиса "
    "«интерфейс» — есть сервис, отвечающий за записи, и сервис, отвечающий за "
    "склад. Граница проходит там, где меняется язык предметной области.", LEAD))
add(Paragraph(
    "У каждого контекста один владелец данных. Если запись создаёт сервис "
    f"{mono('booking')}, то и историей визитов владеет он — карточка клиента "
    "получает её запросом, а не хранит копию. Если контакты клиента находятся в "
    f"{mono('client')}, то {mono('notification')} запрашивает их в момент "
    "отправки. Правило формулируется одной фразой: <b>дублировать чужие данные "
    "нельзя, можно только запрашивать</b>.", BODY))

extend(table_block(
    [
        ["Контекст", "Сервис", "Язык", "Область ответственности"],
        ["Организация", ("core", "mono"), "Python", "компании, филиалы, сотрудники, графики работы"],
        ["Каталог", ("catalog", "mono"), "Python", "услуги, цены по филиалам, нормативы расхода"],
        ["Записи", ("booking", "mono"), "Go", "запись к специалисту, расписание, свободные слоты"],
        ["Склад", ("inventory", "mono"), "Go", "остатки материалов, списание, минимальные пороги"],
        ["Клиенты", ("client", "mono"), "Python", "карточка клиента, контакты, лояльность"],
        ["Расчёты", ("billing", "mono"), "Python", "счета, оплаты, комиссия специалиста"],
        ["Уведомления", ("notification", "mono"), "Go", "напоминания клиентам, алерты администраторам"],
        ["Аналитика", ("analytics", "mono"), "Python", "выручка, загрузка, конверсия, расход материалов"],
    ],
    [28 * mm, 26 * mm, 18 * mm, W - 72 * mm],
    "Ограниченные контексты и соответствующие им сервисы",
))

add(Paragraph("Выбор языка реализации", H3))
add(Paragraph(
    "Go применён там, где он уместен по существу задачи. Захват слота — "
    "конкурентная операция: два администратора могут инициировать запись на одно "
    "время одновременно. Списание материалов — конкурентное изменение счётчика. "
    "Рассыльщик уведомлений должен быть лёгким и удерживать два постоянных "
    "соединения с брокерами. Остальные сервисы — справочники и отчёты, где "
    "выигрывает скорость разработки на Python.", PLAIN))
add(Paragraph(
    "Следствие видно в размере образов: сервисы на Go, собранные на distroless, "
    "занимают 28–34 МБ против 332 МБ у Python. Холодный старт — десятки "
    "миллисекунд против нескольких секунд, поэтому именно сервис на Go назначен "
    "целью для бессерверного развёртывания.", BODY))


# ---------------------------------------------------------------- 3

add(heading("Топология системы"))
add(Paragraph(
    "Система состоит из двух ярусов и двух шин. Верхний ярус — сервисы, к "
    "которым обращаются синхронно; нижний — потребители событий. Наружу смотрит "
    "только шлюз, публичных портов у сервисов нет.", LEAD))

add(figure(diagrams.Topology, 268, "Ярусы системы и шины сообщений"))

add(Paragraph(
    "Синхронные обращения между сервисами образуют отдельный граф. Он намеренно "
    "разрежен: чем меньше синхронных зависимостей, тем меньше вероятность, что "
    "отказ одного сервиса остановит другой.", PLAIN))

add(figure(diagrams.SyncCalls, 186, "Граф синхронных вызовов: стрелка от вызывающего к вызываемому"))

extend(table_block(
    [
        ["Вызов", "Метод", "Назначение"],
        [("booking → core", "mono"), ("GetEmployeeSchedule", "mono"), "график специалиста"],
        [("booking → core", "mono"), ("GetBranch", "mono"), "проверка филиала"],
        [("booking → catalog", "mono"), ("GetService", "mono"), "длительность и цена услуги"],
        [("inventory → catalog", "mono"), ("GetConsumptionNorms", "mono"), "норматив расхода материалов"],
        [("billing → booking", "mono"), ("GetAppointment", "mono"), "детали визита"],
        [("billing → client", "mono"), ("AddLoyaltyPoints", "mono"), "начисление баллов"],
        [("client → booking", "mono"), ("ListClientAppointments", "mono"), "история записей"],
        [("notification → client", "mono"), ("GetClientContacts", "mono"), "контакты и канал связи"],
        [("analytics → core", "mono"), ("GetBranch, ListEmployees", "mono"), "справочник для отчётов"],
    ],
    [40 * mm, 46 * mm, W - 86 * mm],
    "Синхронные вызовы между сервисами",
))


# ---------------------------------------------------------------- 4

add(heading("Транспортные уровни"))
add(Paragraph(
    "В системе четыре канала передачи данных. Каждый отвечает за свой класс "
    "задач, и правило выбора формулируется одной фразой — это существеннее "
    "самих технологий.", LEAD))

extend(table_block(
    [
        ["Канал", "Область применения", "Правило выбора"],
        ["REST", "запросы извне, через шлюз",
         "идёт мимо шлюза — значит не REST"],
        ["gRPC", "синхронные вызовы между сервисами",
         "нужен ответ прямо сейчас"],
        ["RabbitMQ", "доменные события, меняющие состояние",
         "потеря сообщения означает потерю данных"],
        ["NATS", "эфемерные широковещательные обновления",
         "сообщение устаревает за секунды"],
    ],
    [24 * mm, 58 * mm, W - 82 * mm],
    "Транспортные уровни и правила выбора",
))

add(Paragraph("Внешний интерфейс", H3))
add(Paragraph(
    "Всё, что приходит от человека, проходит через шлюз по протоколу HTTP с "
    "телом в формате JSON. Единственный HTTP-переход внутри периметра — от шлюза "
    "к сервису.", PLAIN))

add(Paragraph("Синхронное взаимодействие", H3))
add(Paragraph(
    "Обращения к соседним сервисам выполняются по gRPC: девять методов, все "
    "внутренние. Контракты описаны в файлах формата Protocol Buffers и "
    "генерируются в оба языка, поэтому рассогласование обнаруживается при сборке, "
    "а не во время работы.", PLAIN))

add(Paragraph("Гарантированная доставка событий", H3))
add(Paragraph(
    "Всё, что меняет состояние системы и не может быть потеряно, публикуется в "
    f"обменник типа topic {mono('mirea.events')}. Очереди объявлены как durable, "
    "издатель работает с подтверждениями, потребитель — с ручным подтверждением "
    "обработки. Неразобранные сообщения направляются в отдельную очередь "
    "недоставленных.", PLAIN))

add(Paragraph("Эфемерные сообщения", H3))
add(Paragraph(
    "Широковещательные обновления, потеря которых безвредна, передаются через "
    "NATS: изменение занятости на табло у стойки регистрации и всплывающие "
    "предупреждения администратору. Гарантий доставки нет, подписчик "
    "синхронизирует состояние при переподключении.", PLAIN))

add(note(
    "<b>О наличии двух брокеров.</b> RabbitMQ обеспечивает гарантии доставки "
    "ценой очередей и подтверждений; NATS обеспечивает скорость ценой отсутствия "
    "гарантий. Наглядно различие видно на предупреждении о низком остатке "
    "материала: один и тот же факт передаётся обоими путями — через NATS "
    "мгновенно на экран, через RabbitMQ надёжно для отчётности и уведомления "
    "по электронной почте. В промышленной системе такого масштаба достаточно "
    "одного брокера; второй введён для демонстрации обоих классов доставки, "
    "и слой NATS изолирован так, что удаляется без изменения доменной логики."))


# ---------------------------------------------------------------- 5

add(heading("Доменные события"))
add(Paragraph(
    "В системе десять доменных событий. Имя каждого — глагол в прошедшем "
    "времени: событие уже произошло, отменить его нельзя, можно лишь выпустить "
    "другое, компенсирующее его последствия.", LEAD))

extend(table_block(
    [
        ["Событие", "Публикует", "Потребляют"],
        [("branch.opened", "mono"), ("core", "mono"), ("analytics", "mono")],
        [("employee.hired", "mono"), ("core", "mono"), ("analytics", "mono")],
        [("client.registered", "mono"), ("client", "mono"), ("analytics", "mono")],
        [("appointment.created", "mono"), ("booking", "mono"), ("notification, analytics", "mono")],
        [("appointment.cancelled", "mono"), ("booking", "mono"), ("notification, analytics", "mono")],
        [("appointment.completed", "mono"), ("booking", "mono"), ("inventory, billing, analytics", "mono")],
        [("consumables.written_off", "mono"), ("inventory", "mono"), ("analytics", "mono")],
        [("stock.low", "mono"), ("inventory", "mono"), ("notification, analytics", "mono")],
        [("invoice.issued", "mono"), ("billing", "mono"), ("notification, analytics", "mono")],
        [("invoice.paid", "mono"), ("billing", "mono"), ("analytics", "mono")],
    ],
    [50 * mm, 26 * mm, W - 76 * mm],
    "Каталог доменных событий",
))

add(Paragraph(
    f"Ключевым является событие {mono('appointment.completed')}. Одно событие "
    "расходится на трёх потребителей: склад списывает материалы, расчётный "
    "сервис выставляет счёт, аналитика учитывает выручку. Ни один из них не "
    "знает о существовании остальных, и добавление четвёртого потребителя не "
    f"требует изменений в сервисе {mono('booking')}.", BODY))

add(figure(diagrams.FanOut, 152,
           "Расхождение одного события на независимых потребителей"))

add(Paragraph("Состав сообщения", H3))
add(Paragraph(
    "Событие несёт идентификаторы, а не полный снимок данных. Расчётный сервис "
    "получает идентификатор записи и самостоятельно запрашивает цену и "
    "специалиста. Преимущество: полезная нагрузка не разрастается и не требует "
    "версионирования при каждом изменении карточки визита. Недостаток: "
    "потребитель зависит от доступности издателя в момент обработки. Размен "
    "принят осознанно.", PLAIN))

add(Paragraph("Формат представления", H3))
add(Paragraph(
    "Схемы событий описаны в Protocol Buffers, однако в очередь помещается не "
    "двоичное представление, а JSON, полученный по правилам protobuf-JSON. "
    "Плата — размер сообщения: 289 байт против 116. Приобретение — читаемость: "
    "когда сервис на Go не может разобрать событие от сервиса на Python, в "
    "панели управления брокером виден текст, а не шестнадцатеричный дамп. При "
    "восьми сервисах на двух языках это окупается.", PLAIN))


# ---------------------------------------------------------------- 6

add(heading("Организация данных"))
add(Paragraph(
    "Каждому сервису соответствует отдельная база данных с собственным "
    "пользователем. Изоляция не декларативная: попытка пользователя одного "
    "сервиса открыть чужую базу отклоняется сервером с ошибкой доступа.", LEAD))
add(Paragraph(
    "Физически используется один экземпляр PostgreSQL. Восемь отдельных "
    "экземпляров избыточны для учебного стенда, при этом требование «база на "
    "сервис» соблюдено, и в контекстной карте базы изображаются раздельно.", BODY))
add(Paragraph(
    "Схема во всех сервисах поднимается миграциями, а не автоматическим "
    "созданием таблиц: в Python применяется Alembic, в Go — goose со "
    "встроенными в исполняемый файл SQL-сценариями. Миграции накатываются при "
    "запуске контейнера, поэтому отдельный шаг развёртывания не требуется.", BODY))

add(note(
    "<b>Особенность Alembic.</b> Автоматический генератор миграций помещает в "
    "процедуру отката только удаление таблиц, тогда как перечислимый тип "
    "остаётся в базе. Повторный накат завершается ошибкой существующего "
    "объекта. Проявляется не сразу: первый прогон на чистой базе проходит "
    "успешно. Во всех сервисах, использующих перечислимые типы, удаление "
    "дописано вручную."))


# ---------------------------------------------------------------- 7

add(heading("Сквозные решения"))
add(Paragraph(
    "Восемь механизмов реализованы одинаково во всех сервисах. Именно они "
    "превращают набор программ в систему.", LEAD))

add(heading("Контракты прежде реализации", 1))
add(Paragraph(
    "Каталог с описаниями Protocol Buffers — единственный источник сведений о "
    "границах сервисов. Из него генерируются и классы Python, и структуры Go, "
    "поэтому несовпадение приводит к ошибке сборки. Практическая польза "
    "оказалась шире ожидаемой: сервис записей разрабатывался и тестировался "
    "против подставного каталога услуг, когда настоящий ещё не существовал.", PLAIN))
add(Paragraph(
    "Номера полей в описании и составляют контракт: по каналу связи передаётся "
    "номер, а не имя. Поле допустимо переименовать, номер — нет.", PLAIN))

add(heading("Непрерывность трассировки", 1))
add(Paragraph(
    f"Заголовок {mono('traceparent')} принимается из входящего HTTP-запроса, "
    "передаётся в метаданных gRPC, проставляется в конверт события и "
    "восстанавливается потребителем на другом конце очереди. Один идентификатор "
    "проходит через синхронные вызовы, оба брокера и оба языка.", PLAIN))
add(Paragraph(
    "Без этого события в системе трассировки выглядели бы несвязанными корнями, "
    "и установить, какой запрос породил списание материалов, было бы нечем.", PLAIN))

add(heading("Идемпотентность потребителей", 1))
add(Paragraph(
    "RabbitMQ гарантирует доставку не менее одного раза, поэтому повторная "
    "доставка неизбежна. У каждого потребителя предусмотрена таблица "
    "обработанных событий: обработка начинается со вставки идентификатора.", PLAIN))
add(note(
    "<b>Отметка записывается той же транзакцией, что и результат обработки.</b> "
    "Это не деталь реализации, а суть механизма. При записи отметки отдельной "
    "транзакцией неудачная обработка оставит событие помеченным, и повторная "
    "доставка молча его пропустит. Дефект был обнаружен именно так: списание не "
    "прошло из-за нехватки материала, а событие уже считалось обработанным."))

add(heading("Единая трактовка ошибок", 1))
add(Paragraph(
    "Доменные ошибки объявлены один раз: «не найдено», «конфликт состояния», "
    "«недопустимый аргумент», «сосед недоступен». Каждый транспорт переводит их "
    "в собственные коды по таблице соответствия. В обработчиках запросов разбор "
    "ошибок отсутствует.", PLAIN))

extend(table_block(
    [
        ["Доменная ошибка", "HTTP", "gRPC"],
        ["Не найдено", "404", ("NOT_FOUND", "mono")],
        ["Конфликт состояния", "409", ("ABORTED", "mono")],
        ["Недопустимый аргумент", "422", ("INVALID_ARGUMENT", "mono")],
        ["Сосед недоступен", "503", ("UNAVAILABLE", "mono")],
        ["Нарушение ограничения БД", "409", ("ABORTED", "mono")],
    ],
    [60 * mm, 24 * mm, W - 84 * mm],
    "Соответствие доменных ошибок кодам транспортов",
))
add(Paragraph(
    "Отдельного внимания заслуживает «сосед недоступен». Если недоступен "
    f"{mono('catalog')}, это не внутренняя ошибка {mono('booking')}, и код 500 "
    "неверен: клиенту имеет смысл повторить запрос, а системе мониторинга — не "
    "относить отказ на наш счёт.", PLAIN))

add(heading("Разделение живости и готовности", 1))
add(Paragraph(
    f"Проба {mono('/healthz')} отвечает «процесс не завис» и зависимости не "
    f"проверяет. Проба {mono('/readyz')} проверяет базу данных и брокеры и "
    "возвращает код 503, если что-либо недоступно. При объединении проб "
    "оркестратор либо считает сервис живым при недоступной базе, либо "
    "перезапускает исправный сервис из-за отказа соседа.", PLAIN))

add(heading("Разграничение доступа", 1))
add(Paragraph(
    "Проверка прав выполняется однократно, на шлюзе. Методические указания "
    "допускают и проверку в каждом сервисе, обращающемся к системе управления "
    "доступом, однако при восьми сервисах это означало бы восемь копий "
    "одинакового кода и восемь мест, где его можно реализовать по-разному.", PLAIN))
add(figure(diagrams.AccessControl, 118,
           "Прохождение запроса через шлюз и коды отказа"))
add(Paragraph(
    "Проверяется не только подпись. Токен с верной подписью, но выданный другой "
    "областью или другому клиентскому приложению, — это чужой токен, и подпись "
    f"у него правильная. Отдельно отклоняется токен личности: поле {mono('typ')} "
    "у него иное, тогда как подпись и область совпадают.", PLAIN))
extend(table_block(
    [
        ["Проверяется", "Отклоняется", "Иначе возможно"],
        ["подпись ключом области", "подделанный токен",
         "любой подписанный чем угодно токен считается действительным"],
        ["срок действия", "просроченный токен",
         "отозванный доступ сохраняется бессрочно"],
        ["издатель", "токен чужой области",
         "учётная запись из посторонней области получает доступ"],
        ["получатель", "токен для другой системы",
         "токен, выданный для стороннего сервиса, принимается как свой"],
        ["клиентское приложение", "токен чужого приложения",
         "приложение, зарегистрированное иначе, действует от имени системы"],
    ],
    [40 * mm, 40 * mm, W - 80 * mm],
    "Состав проверки токена",
))
add(Paragraph(
    "После проверки токен дальше не передаётся: за периметром он уже проверен. "
    "Вместо него передаются три заголовка — идентификатор пользователя, имя и "
    "роли, — и те же сведения передаются далее в метаданных gRPC. Одноимённые "
    "заголовки, поступившие извне, затираются: иначе личность подделает любой, "
    "кому известны их наименования.", PLAIN))
add(Paragraph(
    f"Идентификатор пользователя попадает в поле {mono('actor')} конверта "
    "события, поэтому в очереди видно, чьё действие привело к событию. У "
    "событий, порождённых обработкой другого события, поле остаётся пустым — "
    "человека там нет.", PLAIN))
add(Paragraph(
    "Проверка разнесена на два уровня, и это не дублирование: уровни отвечают "
    "на разные вопросы. Роль проверяет шлюз — вправе ли пользователь обращаться "
    "к такому запросу вообще. Принадлежность объекта проверяет владеющий им "
    "сервис — его ли это визит, его ли расписание. Шлюзу принадлежность "
    "неизвестна и известна быть не должна.", PLAIN))
add(Paragraph(
    "Чтобы сервису было с чем сравнивать, шлюз выясняет у сервиса организации, "
    "какой сотрудник соответствует учётной записи, и передаёт его отдельным "
    "заголовком. Запрашивается это лишь для непривилегированных вызывающих: "
    "администратору и управляющему принадлежность безразлична, и обращаться к "
    "справочнику незачем. Ответ удерживается недолгое время — соответствие "
    "меняется редко, а требуется на каждом запросе.", PLAIN))
extend(table_block(
    [
        ["Запрос", "Владелец", "Что проверяется"],
        ["расписание сотрудника", "организация", "специалист видит только своё"],
        ["карточка визита", "записи", "специалист видит только свои визиты"],
        ["закрытие визита", "записи", "закрыть можно только свой визит"],
        ["комиссия сотрудника", "расчёты", "специалист видит только свой заработок"],
        ["загрузка сотрудника", "аналитика", "специалист видит только свою загрузку"],
    ],
    [42 * mm, 30 * mm, W - 72 * mm],
    "Проверки принадлежности объекта",
))
add(note(
    "<b>Проверка при закрытии визита предшествует изменению состояния.</b> "
    "В противном случае событие о завершении успевает уйти в шину, и "
    "потребители начинают списывать материалы и выставлять счёт по чужому "
    "визиту. Отменить это уже нечем: событие доставлено. Отсюда и лишнее "
    "чтение перед изменением — выполняемое, впрочем, только для "
    "непривилегированного вызывающего."
))
add(Paragraph(
    "Учётной записи может не соответствовать ни один сотрудник — обычное дело, "
    "не всякая учётная запись принадлежит работнику. В этом случае доступа к "
    "чужим объектам нет вовсе: отказ по умолчанию, иначе такая запись видела "
    "бы всё.", PLAIN))
add(Paragraph(
    "Отдельного упоминания заслуживает место проверки в сервисе организации. "
    "Она поставлена на границе внешнего интерфейса, а не внутри операции: ту "
    "же операцию вызывает сервис записей, когда вычисляет свободное время "
    "филиала, и чужое расписание там — совершенно законный запрос. Проверка "
    "внутри операции лишила бы специалиста возможности видеть свободное "
    "время коллег.", PLAIN))

add(heading("Наблюдаемость", 1))
add(Paragraph(
    "Распределённая трассировка и сбор показателей подняты вместе с системой. "
    "В распределённой системе это не удобство, а единственный способ ответить "
    "на вопрос, где ушло время: журналы девяти процессов сами по себе в одну "
    "картину не складываются.", PLAIN))
add(figure(diagrams.TraceWaterfall, 172,
           "Трасса одного запроса: участки работы сервисов на общей шкале времени"))
add(Paragraph(
    "Существенно здесь то, что трасса не прерывается ни на границе языков, ни "
    "на границе очереди. Контекст передаётся в поле конверта события, и "
    "потребитель продолжает трассу издателя, а не начинает собственную. "
    "Механизм для этого существовал изначально: заголовок W3C передавался по "
    "всей системе задолго до подключения трассировки, и оставалось связать его "
    "с текущим участком.", PLAIN))
add(Paragraph(
    "Работа, выполняемая после отправки ответа, — списание материалов, "
    "выставление счёта, пересчёт витрин — остаётся в той же трассе. Без этого "
    "асинхронная часть системы оказалась бы вне наблюдения целиком.", PLAIN))
extend(table_block(
    [
        ["Показатель", "Что отражает"],
        ["запросы по маршрутам", "нагрузка по сервисам, маршрутам и кодам ответа"],
        ["время обработки", "распределение, из него вычисляется 95-й процентиль"],
        ["вызовы gRPC", "обращения к соседям по методам и кодам"],
        ["публикация событий", "поток по ключам маршрутизации"],
        ["обработка событий", "исходы: обработано, пропущено, отказ, конверт не разобран"],
        ["состояние базы", "соединения, транзакции и размеры по каждой базе"],
    ],
    [52 * mm, W - 52 * mm],
    "Собираемые показатели",
))
add(Paragraph(
    "Меткой служит шаблон маршрута, а не фактический путь. В противном случае "
    "каждый идентификатор в адресе заводит собственный ряд значений, и объём "
    "хранимых показателей растёт вместе с числом записей в системе.", PLAIN))
add(note(
    "<b>Отдельно учитываются два вида отказа потребителя.</b> Первый — "
    "обработчик вернул ошибку. Второй — конверт события не удалось разобрать "
    "вовсе. Второй случай опаснее: сообщение уходит в очередь недоставленных "
    "молча, и заметить это без отдельного счётчика нечем. Оба вида проверены "
    "отправкой в шину неисполнимого и повреждённого события."
))

add(heading("Непрерывная интеграция и доставка", 1))
add(Paragraph(
    "Конвейер устроен так, что проверки предшествуют всему остальному: образ "
    "не публикуется, пока весь набор тестов не выполнен успешно. Работы "
    "выполняются параллельно по сервисам, поэтому время конвейера определяется "
    "самым медленным сервисом, а не их суммой.", PLAIN))
extend(table_block(
    [
        ["Работа", "Содержание"],
        ["контракты", "проверка описаний и соответствия сгенерированного кода"],
        ["тесты Python", "шесть сервисов: анализатор, модульные, интеграционные"],
        ["тесты Go", "три сервиса: форматирование, анализатор, обе группы тестов"],
        ["публикация", "образ службы склада в общедоступный реестр"],
        ["развёртывание", "справочник услуг бессерверным контейнером"],
    ],
    [40 * mm, W - 40 * mm],
    "Состав конвейера",
))
add(Paragraph(
    "Интеграционным тестам необходима база данных: она поднимается служебным "
    "контейнером, а базы с отдельными пользователями заводит тот же сценарий, "
    "что применяется при локальном запуске. Разделение тестов на группы, "
    "выполненное заранее, здесь окупилось: конвейер поднимает базу отдельным "
    "шагом, и переделывать сценарий не потребовалось.", PLAIN))
add(Paragraph(
    "Проверка соответствия сгенерированного кода описаниям контрактов "
    "заслуживает отдельного упоминания. Изменение описания без последующей "
    "генерации не проявляется у того, кто его внёс, — сборка ломается у "
    "следующего разработчика и в неожиданном месте. Конвейер выполняет "
    "генерацию и сравнивает результат с содержимым хранилища.", PLAIN))
add(Paragraph(
    "Выбор сервисов для двух способов доставки определяется их устройством. "
    "Служба склада удерживает соединение с брокером и подписку на очередь, "
    "поэтому остановка между вызовами для неё недопустима — сообщения "
    "накапливались бы в её отсутствие. Справочник услуг противоположен: "
    "отвечает на запросы, соединений не удерживает и в памяти процесса ничего "
    "не хранит, а потому переживает остановку без последствий.", PLAIN))
add(note(
    "<b>Публикация включается наличием доступов.</b> Учётные данные реестров "
    "хранятся в защищённых переменных хранилища, и при их отсутствии работы "
    "публикации пропускаются, а проверки выполняются полностью. Конвейер "
    "остаётся работоспособным и до настройки доступов, что позволяет "
    "проверять изменения с первого же дня."
))


# ---------------------------------------------------------------- 8

add(heading("Сквозные сценарии"))

add(heading("Запись клиента", 1))
add(Paragraph(
    "Синхронная часть работы системы: три сервиса, два вызова gRPC, одна "
    "проверка на уровне базы данных.", PLAIN))
add(figure(diagrams.Sequence, 200, "Последовательность создания записи"))
add(Paragraph(
    "Существенно, что захват слота выполняется ограничением базы данных, а не "
    "проверкой в коде: между чтением занятых интервалов и вставкой слот может "
    "быть занят другим запросом.", PLAIN))

add(heading("Завершение визита", 1))
add(Paragraph(
    "Асинхронная часть: одно событие, три независимых потребителя.", PLAIN))
extend(numbered([
    f"Специалист закрывает запись, сервис {mono('booking')} публикует событие "
    f"{mono('appointment.completed')}.",
    f"Сервис {mono('inventory')} запрашивает у каталога норматив расхода и "
    "списывает материалы одной транзакцией. При падении остатка ниже порога "
    "выпускает предупреждение в оба брокера.",
    f"Сервис {mono('billing')} запрашивает детали визита, выставляет счёт и "
    "рассчитывает комиссию специалиста.",
    f"Сервис {mono('analytics')} отмечает исход записи в витрине конверсии.",
    f"Сервис {mono('notification')} получает уведомление о счёте и о низком "
    "остатке, запрашивает контакты клиента и выполняет отправку.",
]))
add(Spacer(1, 4))
add(Paragraph(
    "Ни один из потребителей не осведомлён об остальных. Добавление четвёртого "
    "не требует изменений в издателе — достаточно нового связывания в топологии "
    "очередей.", PLAIN))


# ---------------------------------------------------------------- 9 сервисы

add(PageBreak())
add(heading("Описание сервисов"))
add(Paragraph(
    "Далее приводится спецификация каждого сервиса: область ответственности, "
    "принадлежность данных, программный интерфейс и решения, существенные для "
    "понимания его работы.", LEAD))


def service(name, role, meta, owns, not_owns, rest=None, grpc=None, events=None,
            decisions=()):
    add(heading(name, 1))
    add(Paragraph(role, PLAIN))

    rows = [[Paragraph(f"<b>{k}</b>", CELL), Paragraph(v, CELL)] for k, v in meta]
    table = Table(rows, colWidths=[34 * mm, W - 34 * mm], hAlign="LEFT")
    table.setStyle(TableStyle([
        ("VALIGN", (0, 0), (-1, -1), "TOP"),
        ("TOPPADDING", (0, 0), (-1, -1), 2),
        ("BOTTOMPADDING", (0, 0), (-1, -1), 2),
        ("LEFTPADDING", (0, 0), (-1, -1), 0),
        ("LINEABOVE", (0, 0), (-1, 0), 0.6, INK),
        ("LINEBELOW", (0, -1), (-1, -1), 0.6, INK),
    ]))
    add(Spacer(1, 4))
    add(table)
    add(Spacer(1, 7))

    add(Paragraph(f"<b>Владеет.</b> {owns}", PLAIN))
    add(Paragraph(f"<b>Не владеет.</b> {not_owns}", PLAIN))

    if rest:
        add(Paragraph("Программный интерфейс", H3))
        extend(table_block(
            [["Метод и путь", "Назначение"]] + [[(m, "mono"), d] for m, d in rest],
            [72 * mm, W - 72 * mm], head=True,
        ))
    if grpc:
        add(Paragraph("Взаимодействие по gRPC", H3))
        add(Paragraph(grpc, PLAIN))
    if events:
        add(Paragraph("События", H3))
        add(Paragraph(events, PLAIN))
    if decisions:
        add(Paragraph("Существенные решения", H3))
        for text in decisions:
            add(Paragraph(text, PLAIN))
    add(Spacer(1, 6))


service(
    "core-service",
    "Ядро системы и единственный источник сведений об организации: какие "
    "существуют компании, филиалы, сотрудники и по какому графику они работают.",
    [("Язык", "Python, FastAPI"), ("База данных", "core_db"),
     ("Порты", "8001 (HTTP), 9001 (gRPC)"), ("Автотестов", "35")],
    "Компаниями, филиалами, сотрудниками, должностями, графиками работы.",
    "Записями, клиентской базой, услугами. Ядро отвечает на вопрос «кто и где "
    "работает», а не «что происходит».",
    rest=[
        ("POST /companies", "зарегистрировать компанию"),
        ("POST /companies/{id}/branches", "открыть филиал"),
        ("POST /branches/{id}/employees", "принять сотрудника с графиком"),
        ("GET /branches/{id}/employees", "сотрудники филиала, фильтр по роли"),
        ("GET /employees/{id}/schedule", "график специалиста за период"),
    ],
    grpc=f"Предоставляет {mono('GetBranch')} и {mono('GetEmployeeSchedule')} "
         f"сервису записей, {mono('ListEmployees')} — аналитике. Самый "
         "востребованный сервер gRPC в системе.",
    events=f"Публикует {mono('branch.opened')} и {mono('employee.hired')}. "
           "Событий не потребляет: ядро находится в начале цепочки, его "
           "состояние изменяют только пользователи.",
    decisions=[
        "<b>Смены специалиста не пересекаются</b>, и это проверяется дважды: в "
        "агрегате при назначении графика, что даёт содержательное сообщение об "
        "ошибке, и ограничением исключения на уровне базы, что перехватывает "
        "запись в обход приложения. Дублирование намеренно: сервис записей "
        "принимает решение о занятости на основании этого графика, и "
        "пересекающиеся смены нарушили бы его логику. Смены, примыкающие "
        "встык, пересечением не считаются — интервал полуоткрытый.",
        "<b>Роли сотрудников</b> совпадают с ролями в системе управления "
        "доступом. Справочник ядра и роли в токене должны согласовываться.",
    ],
)

service(
    "catalog-service",
    "Справочник услуг: перечень оказываемых услуг, их стоимость, длительность "
    "и норматив расхода материалов на процедуру.",
    [("Язык", "Python, FastAPI"), ("База данных", "catalog_db"),
     ("Порты", "8002 (HTTP), 9002 (gRPC)"), ("Автотестов", "13")],
    "Услугами, ценами по филиалам, длительностью процедуры, справочником "
    "материалов и нормативами расхода.",
    "Остатками на складе. Здесь норматив «сколько должно уйти», там факт "
    "«сколько есть и сколько израсходовано».",
    rest=[
        ("POST /services", "завести услугу вместе с нормативами"),
        ("GET /services/{id}", "карточка услуги"),
        ("GET /branches/{id}/services", "прайс-лист филиала"),
        ("PUT /services/{id}/price", "изменить цену"),
    ],
    grpc=f"{mono('GetService')} — длительность и цена при создании записи; "
         f"{mono('GetConsumptionNorms')} — состав и количество материалов для "
         "списания после визита.",
    events="Событий не публикует и не потребляет. Не каждый сервис обязан "
           "участвовать в шине: это справочник, изменяемый только "
           "пользователями.",
    decisions=[
        "<b>Норматив расхода — содержательное ядро сервиса.</b> Он связывает "
        "услугу со складом и делает списание автоматическим: администратор не "
        "отмечает вручную израсходованное количество, оно следует из услуги.",
        "<b>Отдельного интерфейса управления материалами нет.</b> Материалы "
        "заводятся в ходе создания услуги и переиспользуются по наименованию, "
        "поскольку администратор мыслит услугой, а не складской номенклатурой.",
        "<b>Идентификатор филиала в запросе gRPC — проверка, а не фильтр.</b> "
        "Услугу нельзя запросить от имени чужого филиала.",
    ],
)

service(
    "booking-service",
    "Центральный сервис предметной области: запись клиентов к специалистам, "
    "расписание, расчёт свободных слотов. Единственный сервис, задействующий "
    "все четыре транспортных уровня.",
    [("Язык", "Go, chi, pgx"), ("База данных", "booking_db"),
     ("Порты", "8003 (HTTP), 9003 (gRPC)"), ("Автотестов", "21")],
    "Записями, занятостью слотов, историей визитов клиента.",
    "Графиком специалиста и характеристиками услуги. Оба справочника "
    "запрашиваются синхронно в момент создания записи.",
    rest=[
        ("GET /branches/{id}/slots", "свободные слоты на период"),
        ("POST /appointments", "записать клиента"),
        ("GET /appointments/{id}", "детали записи"),
        ("POST /appointments/{id}/complete", "закрытие визита специалистом"),
        ("POST /appointments/{id}/cancel", "отмена с указанием причины"),
    ],
    grpc=f"Предоставляет {mono('GetAppointment')} расчётному сервису и "
         f"{mono('ListClientAppointments')} — клиентскому. Сам обращается к "
         "ядру за графиком и к каталогу за услугой.",
    events=f"Публикует {mono('appointment.created')}, "
           f"{mono('appointment.cancelled')}, {mono('appointment.completed')} "
           "в RabbitMQ и изменение занятости слота в NATS.",
    decisions=[
        "<b>Состязание за слот разрешается базой данных.</b> Два "
        "администратора могут инициировать запись одновременно, и проверка в "
        "коде от этого не защищает. Ограничение исключения по специалисту и "
        "временному интервалу не позволит вставить пересекающуюся запись; "
        "проигравшая сторона получает код 409 с содержательным сообщением, а "
        "не код 500.",
        "<b>Отменённая запись освобождает слот, завершённая — нет.</b> Условие "
        "входит в само ограничение. Ошибка в нём заблокировала бы время после "
        "каждой отмены, причём без внешних признаков, поэтому оно закрыто "
        "интеграционным тестом.",
        "<b>Сетка расписания получасовая.</b> Смена, начинающаяся в 9:10, даёт "
        "первый слот в 9:30, иначе расписание распадается на непригодные "
        "промежутки. Запись должна целиком помещаться в одну смену.",
        "<b>Цена фиксируется в момент записи.</b> Прайс-лист может измениться "
        "до визита, а счёт выставляется по цене, на которую клиент согласился.",
    ],
)

service(
    "inventory-service",
    "Склад материалов филиала: списание по факту оказанной услуги, пополнение, "
    "контроль остатков. Первый потребитель событий в системе.",
    [("Язык", "Go, chi, pgx"), ("База данных", "inventory_db"),
     ("Порт", "8004 (HTTP)"), ("Автотестов", "20")],
    "Остатками материалов по филиалам, историей движений, минимальными порогами.",
    "Справочником материалов и нормативами расхода — они принадлежат каталогу. "
    "Здесь учитываются только количества.",
    rest=[
        ("GET /branches/{id}/stock", "остатки филиала"),
        ("GET /branches/{id}/stock/low", "позиции ниже порога"),
        ("POST /branches/{id}/stock/replenish", "пополнить склад"),
        ("PUT /branches/{id}/stock/{cid}/threshold", "задать минимальный порог"),
    ],
    grpc="Обращается к каталогу за нормативом расхода. Собственного сервера "
         "gRPC не имеет.",
    events=f"Потребляет {mono('appointment.completed')}. Публикует "
           f"{mono('consumables.written_off')} и {mono('stock.low')}, а также "
           "предупреждение в NATS.",
    decisions=[
        "<b>Списание и пополнение реализованы разными запросами.</b> Одной "
        "операцией вставки с разрешением конфликта они не выполняются: "
        "ограничение неотрицательности проверяется на предлагаемой строке до "
        "разрешения конфликта, а конструкция разрешения конфликта "
        "перехватывает только нарушения уникальности. При списании сервер "
        "обнаружит отрицательное количество и завершит операцию ошибкой, не "
        "дойдя до обновления. Поэтому пополнение создаёт позицию, а списание "
        "лишь уменьшает существующую, что соответствует и смыслу операции.",
        "<b>Все движения выполняются одной транзакцией.</b> Частично списанный "
        "набор материалов хуже, чем не списанный вовсе.",
        "<b>Нехватка материала — тупиковая ситуация, а не повод для повтора.</b> "
        "Событие направляется в очередь недоставленных и требует вмешательства: "
        "пополнить склад и повторить обработку.",
        "<b>Один факт передаётся в оба брокера по-разному.</b> Низкий остаток "
        "уходит в RabbitMQ как доменное событие для отчётности и уведомления, "
        "в NATS — как мгновенное предупреждение на экран.",
    ],
)

service(
    "client-service",
    "Клиентская база: карточка клиента, контактные данные, программа "
    "лояльности. Первый сервис, одновременно являющийся и сервером, и клиентом "
    "gRPC.",
    [("Язык", "Python, FastAPI"), ("База данных", "client_db"),
     ("Порты", "8005 (HTTP), 9005 (gRPC)"), ("Автотестов", "14")],
    "Персональными данными клиента, контактами, предпочитаемым каналом связи, "
    "балансом и историей начисления баллов.",
    "Историей записей — она принадлежит сервису записей. Карточка получает её "
    "по gRPC, а не хранит копию.",
    rest=[
        ("POST /clients", "завести клиента"),
        ("GET /clients?phone=", "поиск по телефону"),
        ("GET /clients/{id}", "карточка с историей записей"),
        ("GET /clients/{id}/loyalty", "баланс и история начислений"),
    ],
    grpc=f"Предоставляет {mono('GetClientContacts')} сервису уведомлений и "
         f"{mono('AddLoyaltyPoints')} — расчётному. Сам запрашивает историю "
         "записей у сервиса записей.",
    events=f"Публикует {mono('client.registered')}. Событий не потребляет.",
    decisions=[
        "<b>Баллы начисляет не этот сервис.</b> Сумму оплаты знает расчётный "
        "сервис, поэтому он и инициирует начисление, тогда как хранит баллы "
        "клиентский. Разделение «кто знает» и «кто хранит» проходит через всю "
        "систему.",
        "<b>Начисление идемпотентно по номеру счёта.</b> Расчётный сервис "
        "может повторить вызов при повторной попытке, но повторное начисление "
        "вернёт нулевое приращение и текущий баланс. Состязание двух "
        "одновременных попыток разрешается уникальным индексом.",
        "<b>Единственное место хранения персональных данных.</b> Это заметно "
        "упрощает разграничение доступа: защищать требуется один сервис.",
    ],
)

service(
    "billing-service",
    "Расчёты за визит: выставление счёта по завершённой записи, отметка "
    "оплаты, расчёт комиссии специалиста.",
    [("Язык", "Python, FastAPI"), ("База данных", "billing_db"),
     ("Порт", "8006 (HTTP)"), ("Автотестов", "15")],
    "Счетами, платежами, расчётом комиссии специалиста.",
    "Ценой услуги и балансом баллов клиента. Первое запрашивается у сервиса "
    "записей, второе изменяется через клиентский сервис.",
    rest=[
        ("GET /invoices/{id}", "счёт"),
        ("POST /invoices/{id}/pay", "отметить оплату"),
        ("GET /branches/{id}/invoices", "счета филиала, фильтр по статусу"),
        ("GET /employees/{id}/commission", "комиссия специалиста за период"),
    ],
    grpc="Обращается к сервису записей за деталями визита и к клиентскому "
         "сервису за начислением баллов. Собственного сервера gRPC не имеет.",
    events=f"Потребляет {mono('appointment.completed')}. Публикует "
           f"{mono('invoice.issued')} и {mono('invoice.paid')}.",
    decisions=[
        "<b>Комиссия начисляется только по оплаченным счетам.</b> Выставленный, "
        "но не оплаченный счёт комиссии не порождает — иначе специалисту "
        "начислялось бы вознаграждение за неполученные деньги.",
        "<b>Идемпотентность обеспечена на двух уровнях.</b> Таблица "
        "обработанных событий не допускает повторной обработки, а уникальность "
        "идентификатора записи в счетах служит страховкой на уровне схемы. "
        "Один визит — один счёт.",
        "<b>Отметка оплаты — синхронная операция с побочным эффектом у "
        "соседа.</b> Она инициирует начисление баллов и только затем публикует "
        "событие оплаты. Порядок существен: событие означает, что все действия "
        "уже выполнены.",
    ],
)

service(
    "notification-service",
    "Взаимодействие с внешними каналами связи: напоминания клиентам, "
    "предупреждения администраторам филиала. Единственный сервис, слушающий "
    "оба брокера, и единственный без базы данных.",
    [("Язык", "Go, chi"), ("База данных", "не используется"),
     ("Порт", "8007 (HTTP)"), ("Автотестов", "11")],
    "Шаблонами сообщений и логикой выбора канала связи.",
    "Контактными данными клиента — они запрашиваются в момент отправки. "
    "Собственного состояния сервис не имеет вовсе.",
    rest=[
        ("POST /notifications", "отправить произвольное уведомление"),
        ("GET /notifications/{id}", "статус отправки"),
        ("GET /templates", "перечень шаблонов"),
    ],
    grpc="Запрашивает контакты клиента у клиентского сервиса.",
    events=f"Потребляет {mono('appointment.created')}, "
           f"{mono('appointment.cancelled')}, {mono('invoice.issued')}, "
           f"{mono('stock.low')} из RabbitMQ и предупреждения из NATS.",
    decisions=[
        "<b>Выбор канала учитывает предпочтение клиента, но допускает "
        "замену.</b> Незаполненный адрес предпочитаемого канала не является "
        "основанием отказаться от отправки. Администратору филиала личные "
        "контакты не требуются, его сообщения выводятся на экран у стойки "
        "регистрации.",
        "<b>Два вида отказов трактуются по-разному, и это главное решение "
        "сервиса.</b> Недоступность почтового сервера помечает отправку "
        "неудачной, но ошибка наружу не поднимается: поток событий важнее "
        "одного сообщения. Недоступность клиентского сервиса поднимает ошибку, "
        "и событие направляется в очередь недоставленных: без контактных "
        "данных отправка невозможна, а повтор осмыслен. Отсутствие различия "
        "приводит либо к переполнению очереди из-за отказа почтового сервера, "
        "либо к безвозвратной потере уведомлений.",
        "<b>Отсутствие базы данных обоснованно.</b> Собственного состояния у "
        "сервиса нет, журнал отправок хранится в кольцевом буфере на 500 "
        "записей и теряется при перезапуске. Отсюда и наименьший образ в "
        "системе — 28 МБ.",
    ],
)

service(
    "analytics-service",
    "Отчётность по сети: выручка филиалов, загрузка специалистов, расход "
    "материалов, конверсия записей. Единственный подписчик на весь поток "
    "событий.",
    [("Язык", "Python, FastAPI"), ("База данных", "analytics_db"),
     ("Порт", "8008 (HTTP)"), ("Автотестов", "16")],
    "Витринами данных. Собственных бизнес-фактов не создаёт: всё состояние "
    "производное.",
    "Ничем первичным. Любой показатель выводится из событий; в чужие базы "
    "данных аналитика не обращается.",
    rest=[
        ("GET /branches/{id}/revenue", "выручка, число счетов, средний чек"),
        ("GET /employees/{id}/workload", "записи, завершённые, занятое время, комиссия"),
        ("GET /branches/{id}/funnel", "конверсия записей"),
        ("GET /reports/consumables", "расход материалов по позициям"),
    ],
    grpc="Обращается к ядру за наименованиями филиалов и сотрудников: витрины "
         "хранят только идентификаторы.",
    events="Потребляет весь поток событий системы.",
    decisions=[
        "<b>Время факта берётся из события, а не из момента записи.</b> "
        "Первоначально использовалась отметка времени по умолчанию, и дефект "
        "был обнаружен тестом: при отставании сервиса и последующей обработке "
        "накопленной очереди все факты пришлись бы на дату обработки, исказив "
        "отчётность за период. Показатели при этом сохраняли бы правдоподобие.",
        "<b>Одна строка на сущность, а не на событие.</b> Витрина записей "
        "хранит жизненный цикл, поэтому конверсия вычисляется группировкой по "
        "состоянию, а не вычитанием счётчиков.",
        "<b>Событие исхода может опередить событие создания.</b> Очереди у "
        "сервисов разные, порядок между ними не гарантирован. При отсутствии "
        "факта записи завершение не выполняет никаких действий.",
        "<b>Выручка учитывается по оплаченным счетам, занятое время — по "
        "завершённым записям, незавершённые записи не влияют на конверсию.</b> "
        "Каждое из трёх правил изменяет значение в отчёте.",
    ],
)

service(
    "gateway",
    "Единственная точка входа: проверка токена и маршрутизация. "
    "Инфраструктурный компонент, в число восьми микросервисов не входит, "
    "собственной предметной области не имеет.",
    [("Язык", "Python, FastAPI"), ("База данных", "не используется"),
     ("Порты", "8000 — единственный, открытый вовне"),
     ("Автотестов", "50"), ("Относится к работе", "ПР9")],
    "Проверкой прав доступа и таблицей маршрутов.",
    "Никакими данными предметной области.",
    rest=[
        ("POST /auth/token", "Выдача токена по имени и паролю"),
        ("GET /routes", "Таблица маршрутов с требуемыми ролями"),
        ("* /...", "Всё остальное передаётся в сервисы"),
    ],
    decisions=[
        "<b>Обоснование отдельного компонента.</b> Ядром системы задуман "
        f"{mono('core')}, однако проксирование через него чужих запросов "
        "нежелательно: обновление ядра остановило бы всю систему, а в "
        "трассировке каждый запрос проходил бы через ядро дважды. Это и есть "
        "распределённый монолит, за который микросервисную архитектуру "
        "критикуют.",
        "<b>Разделение ролей.</b> Шлюз остаётся тонким, без бизнес-логики. "
        f"Сервис {mono('core')} сохраняет положение центра в более существенном "
        "смысле — как единственный источник сведений об организации, к которому "
        "синхронно обращаются остальные.",
        "<b>Передача личности пользователя.</b> Внутри системы сервисы "
        "взаимодействуют по gRPC, и без передачи сведений о пользователе "
        "внутренняя сеть окажется полностью доверенной: получивший доступ "
        "внутрь периметра сможет действовать от чужого имени.",
        "<b>Два адреса системы управления доступом.</b> В поле издателя токена "
        "проставляется тот адрес, по которому обратился браузер, тогда как "
        "ключи проверки подписи шлюз получает изнутри сети. Адреса различны, и "
        "сверять поле издателя следует с внешним. В противном случае токен, "
        "полученный изнутри сети, оказывается выданным иным издателем, нежели "
        "токен из браузера, и проверка их не сводит.",
        "<b>Таблица маршрутов как модель прав.</b> Соответствие «путь — сервис "
        "— требуемая роль» описано в одном месте и доступно для чтения по "
        "отдельному адресу. Права системы читаются целиком, а не собираются по "
        "восьми хранилищам исходного кода.",
    ],
)


# ---------------------------------------------------------------- 10

add(heading("Незавершённые работы"))
add(Paragraph(
    "Все восемь сервисов реализованы, покрыты автоматическими тестами и "
    "работают совместно. Доступ извне закрыт шлюзом, система наблюдаема, "
    "конвейер выполняет проверки на каждом изменении. Сквозные слои, "
    "выполняемые однократно для всей системы, завершены; оставшееся "
    "сводится к настройке внешних учётных данных.", LEAD))

extend(table_block(
    [
        ["Что осталось", "Почему не выполнено сейчас"],
        ["Публикация образов",
         "работы конвейера написаны и проверены, включаются наличием учётных "
         "данных внешних реестров"],
    ],
    [46 * mm, W - 46 * mm],
    "Оставшиеся работы",
))

add(Paragraph(
    "Оставшийся пункт не требует изменений в коде системы: работы конвейера "
    "написаны и проверены, публикация включается появлением учётных данных "
    "внешних реестров. Публичный интерфейс описан спецификацией OpenAPI, "
    "полнота которой проверяется конвейером: маршрут, добавленный в шлюз "
    "мимо описания, роняет сборку.", BODY))

# ---------------------------------------------------------------- сборка

doc = Report("architecture.pdf")
doc.multiBuild(story)
print("готово")
